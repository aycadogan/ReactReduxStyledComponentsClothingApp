//initial state
const INITIAL_STATE = {
    cartItems: [],
    hidden: true,
  }
  
  //reducer function
  const cartReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
      default:
        return state
    }
  }
  
  export default cartReducer